

import App from '@/components/playlist';
import PlaylistHistory from '@/components/playlist-history';
import { NextPage } from 'next';

interface Props { }

const Page: NextPage<Props> = ({ }) => {

    return <App />
};

export default Page;