"use client";
import ListTag from "@/components/admin/tags";
import { NextPage } from "next";

interface Props { }


const Page: NextPage<Props> = ({ }) => {
    return <ListTag />
};

export default Page;
