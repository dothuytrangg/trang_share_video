
'use client'
import ListAccount from '@/components/admin/account';
import { NextPage } from 'next';

interface Props { }

const Page: NextPage<Props> = ({ }) => {
      // const trang = "hello"
      return <ListAccount />
};

export default Page;