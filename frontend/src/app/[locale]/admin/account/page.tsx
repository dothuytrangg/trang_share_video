
'use client'
import ListAccount from '@/components/admin/account';
import { NextPage } from 'next';

interface Props { }

const Page: NextPage<Props> = ({ }) => {
      return <ListAccount/>
};

export default Page;