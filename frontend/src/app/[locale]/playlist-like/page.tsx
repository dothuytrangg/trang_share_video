
import LikePlayList from '@/components/playlist-like';
import { NextPage } from 'next';

interface Props { }

const Page: NextPage<Props> = ({ }) => {
    return <LikePlayList />
};

export default Page;