import { masterSlice } from "@/stores/features/masterSlice"
import { useSelector } from "react-redux"

const PlaylistHistory= () => {
    // const masterStore = useSelector(masterSlice.actions.setIsAdmin);
    return(
        <h1>hello</h1>
    )

}

export default PlaylistHistory